apiVersion: v1
kind: Service
metadata:
  name: {{ include "makkah-bus.fullname" . }}
  labels:
    {{- include "makkah-bus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "makkah-bus.selectorLabels" . | nindent 4 }}
