apiVersion: v1
kind: Service
metadata:
  name: maplocator-snapshot-service
  namespace: urbi
  labels:
    app.kubernetes.io/instance: maplocator-snapshot
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: maplocator-snapshot
spec:
  selector:
    app.kubernetes.io/instance: maplocator-snapshot
    app.kubernetes.io/name: maplocator-snapshot
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
