{{ .Values.name }} has been deployed to namespace {{ .Values.namespace }}.

To check the status:
  kubectl get pods -n {{ .Values.namespace }} -l app.kubernetes.io/name={{ .Values.name }}
  kubectl get svc -n {{ .Values.namespace }} {{ .Values.name }}

To verify the API health:
  kubectl port-forward svc/{{ .Values.name }} 8080:{{ .Values.api.service.port }} -n {{ .Values.namespace }}
  curl http://localhost:8080/health

{{- if .Values.api.ingress.enabled }}

Ingress is enabled. Access the API at:
  {{- range .Values.api.ingress.hosts }}
  https://{{ .host }}
  {{- end }}
{{- end }}
