apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "REGA_RBAC_Notifications_BE.fullname" . }}-test-connection"
  labels:
    {{- include "REGA_RBAC_Notifications_BE.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "REGA_RBAC_Notifications_BE.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
