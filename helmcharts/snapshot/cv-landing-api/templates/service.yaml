apiVersion: v1
kind: Service
metadata:
  name: {{ include "cv-landing-api.fullname" . }}
  labels:
    {{- include "cv-landing-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cv-landing-api.selectorLabels" . | nindent 4 }}
