apiVersion: v1
kind: Service
metadata:
  name: cityview-middleware
  namespace: urbi
  labels:
    app: cityview-middleware
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: cityview-middleware
