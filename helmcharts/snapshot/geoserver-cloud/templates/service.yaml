apiVersion: v1
kind: Service
metadata:
  name: {{ include "geoserver-cloud.fullname" . }}
  labels:
    {{- include "geoserver-cloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "geoserver-cloud.selectorLabels" . | nindent 4 }}
