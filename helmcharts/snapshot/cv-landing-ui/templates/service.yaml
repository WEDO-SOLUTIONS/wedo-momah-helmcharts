apiVersion: v1
kind: Service
metadata:
  name: {{ include "cv-landing-ui.fullname" . }}
  labels:
    {{- include "cv-landing-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cv-landing-ui.selectorLabels" . | nindent 4 }}
