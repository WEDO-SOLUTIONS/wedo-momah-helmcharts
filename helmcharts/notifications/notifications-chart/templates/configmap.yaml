apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name }}-config
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.name }}
data:
{{- range .Values.env }}
  {{ .name }}: {{ .value | quote }}
{{- end }}